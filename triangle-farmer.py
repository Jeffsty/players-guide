# Write a program that lets you input the triagnle's base size and height
# Compute the area of a triangle by turning the above equation into code
# Write the result of the computation

base = input("Base: ")
height = input("Height: ")

area = (float(base) * float(height))/2
print(float(area))